#include <stdio.h>
#include <conio.h>

void main(){
	float area, r;
	printf("Enter the radius of the circle: ");
	scanf("%f",&r);
	area=(22.0/7.0)*r*r;
	printf("Area of the circle is %f is %f",r,area);
	getch();
	
}
